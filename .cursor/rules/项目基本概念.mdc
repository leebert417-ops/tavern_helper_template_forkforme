---
description: 项目基本概念
alwaysApply: true
---

酒馆助手是一个辅助角色扮演AI "SillyTavern" 的一个前端助手，提供了以命令行为入口的前端界面，自定义脚本，MCP工具功能

工作区和配置路径：均为 `${project_workspace}/../../../` 

项目中的`*.yaml`文件可能代表
1. 角色卡 `角色卡.yaml` 或 `角色.yaml` 。包含角色信息，如：世界观、剧情、对话示例等...在其中包含了内嵌的世界书，变量等配置。
2. 世界书 `世界书.yaml`。内部包含了许多的世界书条目，当用户输入触发了对应条目关键词时就会把世界书条目的内容插入到提示词中。
3. 脚本注册器 `脚本.yaml` 或`脚本注册器.yaml` 或 `代码注册器.yaml`。内部包含了许多脚本。所有脚本注册器及内嵌脚本都会被注册，哪怕不运行注册器。
4. 界面 `界面.yaml`。内部用于描述如何绘制html用户界面
5. 变量 `变量.yaml`。这不是标准的，但是是推荐的命名方式，用于存储变量。具体有哪些变量参考`酒馆变量.mdc`
6. 一条索引 `index.yaml`。本质上是角色卡，但由于可能文件名并非角色，从文件名无法判断是否是角色卡。

本项目内每一个子文件夹一般就是一个完整的角色卡扩展包，包含：
- `index.yaml`：角色卡
- 角色卡立绘 `角色名.png`  或 `头像.png`
- `世界书/`：文件夹，包含了各种世界书
- `脚本/`：文件夹，包含了脚本。所有脚本注册器及内嵌脚本都会被注册，哪怕不运行注册器。
- `界面/`：文件夹，界面相关 (html)
- `第一条消息/`：文件夹，仅一个文件 0.txt。用于给酒馆提供第一条消息，但本项目不依赖这个，而是通过脚本注入。

## 项目目录树
- `${project_workspace}/../../../`：工作区及配置目录
  - `data/default-user/extensions/`：扩展文件夹
    - `tavern_helper_forkforme/`：酒馆助手扩展
      - `src/`：源码文件夹，**每一个子文件夹都是一个角色卡扩展包**
        - `模版/`：一些参考用的模版
      - `.cursor/`：codex规则和开发文档(mdc文件)
  

## 约束
无论在任何情况下都必须遵守以下约束：
- 由于脚本是嵌入到yaml中的，所以**不能使用任何换行**，这会导致yaml解析失败；但前端html中无此限制。
- **所有**变量存储，修改，读取**只能**通过`酒馆变量.mdc`中的变量函数，绝对不能使用`variables`或`state`等直接赋值。
- 所有html必须是一个完整的html，不能使用任何外部资源，包括cdn、外部图片、外部字体等，可以使用本地资源（如果有图片，需要先通过prompt告诉用户怎么下载到本地（如果不用imagegen skill的话））。
- html界面中绝对不能把变量写入到全局即window中，这会导致严重的漏洞：会被第三方检查全局变量从而知晓用户当前的状态。

## 角色卡开发指南
在角色卡中包含了三个重要的部分
1. 角色描述。最重要，控制角色如何行动
2. 第一条消息( first message )。如果不使用`第一条消息`功能，那么可以不填第一条消息且可以将其从角色卡扩展包中移除
3. 世界书。世界书规范请看`mvu角色卡.mdc`和`世界书条目结构`部分

## 世界书条目结构
包含：
- 触发词(keys)：一个列表，当用户输入中包含了这些词时，该条目就会被激活。
- 内容(content)：当条目被激活时，会将该内容插入到提示词中。
- 位置(position)：表示该内容是插入到提示词的哪里，是系统提示词中，还是聊天记录的前面，还是聊天记录的后面。
- 触发概率(chance)：一个0-100的整数，表示该条目被触发的概率。
- 层级/优先级(order)：一个整数，表示该条目的优先级。优先级越高的条目越先被插入。
- 注释(comment)：一个字符串，用于注释该条目的作用。
- 二次解释(secondary)：一个字符串，
- [扩展]策略(if通过策略)：激活该世界书条目的策略，有`无`、`全局脚本策略`、`经典策略`、`MVU策略`。

策略的解释：
- 无：表示不通过任何策略，若被激活，直接插入
- 全局脚本策略：激活与否由脚本决定
- MVU策略：激活与否由MVU变量框架决定

## 代码规范
永远使用**绝对路径**(`${project_workspace}/../../../`)，对角色卡扩展包内的资源文件拼接路径。
当你需要查找某个资源是否存在于项目中时，使用工具搜索文件。
使用`async`函数。
